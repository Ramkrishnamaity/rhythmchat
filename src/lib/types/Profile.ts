

export type ProfileFormData = {
    firstName: string
    lastName: string
    about: string,
    image: string
}

export type ProfileResponceType = {
    about: string
    firstName: string
    lastName: string
    email: string
    image: string
    updatedOn: Date
}